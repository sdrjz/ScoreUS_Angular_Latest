import { ObjetoArrayPipe } from './objeto-array.pipe';

describe('ObjetoArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetoArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
